import React from 'react';

// This component is no longer used in the application flow.
// It is kept as an empty component to avoid breaking any potential (though unlikely) remaining imports.
// The new flow goes from AuthScreen directly to the main Chat interface in App.tsx.
const StartScreen: React.FC = () => {
  return null;
};

export default StartScreen;